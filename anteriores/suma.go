package main

import "fmt"

func sumar(a int, b int) int {
	return a + b
}

func main() {
	fmt.Println(sumar(6, 5))
}

/* En el siguiente ejemplo tenemos una función
que suma 2 parámetros enteros recibidos, y que retorna
 también un valor entero. */
